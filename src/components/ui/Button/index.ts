export * from './IconButton';

export enum ButtonColorType {
  inherit = 'inherit',
  default = 'default',
  primary = 'primary',
  secondary = 'secondary',
  error = 'error',
  info = 'info',
  success = 'success',
  warning = 'warning',
}

export enum ButtonSizeType {
  small = 'small',
  medium = 'medium',
  large = 'large',
}
